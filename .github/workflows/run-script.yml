name: Run Script Example

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    # - name: Checkout repository
    #   uses: actions/checkout@v2

    # - name: Set up Node.js (สำหรับตัวอย่างนี้ใช้ Node.js)
    #   uses: actions/setup-node@v2
    #   with:
    #     node-version: '14'  # คุณสามารถตั้งค่า version ของ Node.js ที่ต้องการ

    # - name: Install dependencies (เช่น ติดตั้ง package ต่าง ๆ)
    #   run: |
    #     npm install

    - name: Run script (รันไฟล์ที่คุณต้องการ)
      run: |
        node index.html
